<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginStart="7dp"
    android:layout_marginTop="7dp"
    android:layout_marginEnd="7dp"
    android:layout_marginBottom="7dp"
    android:background="@drawable/card_bg"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:orientation="vertical">

    <!--
    Poster ကို screen width အလိုက် 2:3 ratio ဖြင့်
    အလိုအလျောက်တိုင်းပေးမည်။
-->
<com.cmflix.nativeapp.PosterAspectFrameLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/poster_rounded_bg"
    android:clipChildren="true"
    android:clipToOutline="true"
    android:outlineProvider="background">


        <ImageView
    android:id="@+id/poster"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#20232A"
    android:contentDescription="Movie poster"
    android:scaleType="fitCenter" />


        <TextView
            android:id="@+id/vipRibbon"
            android:layout_width="wrap_content"
            android:layout_height="22dp"
            android:layout_gravity="top|end"
            android:layout_marginTop="6dp"
            android:layout_marginEnd="6dp"
            android:background="@drawable/vip_badge_bg"
            android:elevation="7dp"
            android:gravity="center"
            android:minWidth="38dp"
            android:paddingStart="8dp"
            android:paddingEnd="8dp"
            android:text="VIP"
            android:textColor="@color/white"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="gone" />

        <ImageButton
            android:id="@+id/favoriteRemove"
            android:layout_width="34dp"
            android:layout_height="34dp"
            android:layout_gravity="top|end"
            android:layout_margin="6dp"
            android:background="@drawable/favorite_remove_bg"
            android:contentDescription="Remove from favorites"
            android:elevation="8dp"
            android:padding="8dp"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            android:tint="@color/white"
            android:visibility="gone" />

        <ProgressBar
            android:id="@+id/watchProgress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_gravity="bottom"
            android:indeterminate="false"
            android:max="1000"
            android:progress="0"
            android:progressBackgroundTint="#66000000"
            android:progressTint="@color/brand_red"
            android:visibility="gone" />

    </com.cmflix.nativeapp.PosterAspectFrameLayout>


    <!-- Movie name သီးသန့်သာ ပြမည် -->
    <TextView
        android:id="@+id/movieTitle"
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:ellipsize="end"
        android:gravity="center_vertical"
        android:maxLines="2"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:textColor="@color/white"
        android:textSize="12sp"
        android:textStyle="bold" />

    <!--
        TitleAdapter.java က meta ID ကိုအသုံးပြုထားဆဲဖြစ်သောကြောင့်
        View ကို မဖျက်ဘဲ GONE လုပ်ထားသည်။
        Year/rating စာလည်း မပေါ်ဘဲ နေရာလည်း မယူပါ။
    -->
    <TextView
        android:id="@+id/meta"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:visibility="gone" />

</LinearLayout>
